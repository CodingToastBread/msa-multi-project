spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///Users/codingtoast/IdeaProjects/git-local-repo
          default-label: main
server:
  port: 8888
